# 💾 สำรองข้อมูล n8n + จัดการเครดิต Google Cloud

---

## 1️⃣ เช็กว่าเครดิตหมดวันไหน

**Google Cloud Free Trial = เครดิต $300 ใช้ได้ 90 วัน** (นับจากวันสมัคร)

**ดูวันหมดที่ไหน:**
1. เข้า https://console.cloud.google.com/billing
2. เลือกบัญชีที่ขึ้นว่า **"Free trial"**
3. จะเห็นกล่องบอก **เครดิตคงเหลือ** และ **จำนวนวันที่เหลือ** ชัดเจน

หรือลัด: หน้า Billing → **Overview** → มุมขวาบนมีแถบ "Free trial credit: $XXX remaining, XX days left"

---

## ⚠️ สิ่งที่จะเกิดขึ้นเมื่อหมด 90 วัน

> **บัญชีจะถูกปิด และ VM ทุกตัวจะถูกหยุด (stopped) รวมถึงข้อมูลจะถูกลบในภายหลัง**
> แปลว่า **n8n และ workflow ทั้งหมดหายหมด** ถ้าไม่ได้สำรองไว้

ทางเลือกเมื่อใกล้หมด:
- **อัปเกรดเป็นบัญชี Paid** → เก็บทุกอย่างไว้ + เครดิตที่เหลือยังใช้ได้
- **ย้ายไปเครื่องอื่น** → ต้องสำรองข้อมูลก่อน (ดูข้อ 2)

---

## 💡 เคล็ดลับ: ทำให้ฟรีตลอดไปได้

Google Cloud มี **Free Tier** (คนละอันกับ Free Trial) ที่ให้ **VM ฟรีตลอดชีพ 1 เครื่อง** เงื่อนไข:

| เงื่อนไข | ต้องเป็น |
|---|---|
| ประเภทเครื่อง | **e2-micro** |
| โซน | **us-west1 / us-central1 / us-east1** เท่านั้น |
| ดิสก์ | ไม่เกิน 30 GB |

**เช็กเครื่องคุณ:** Compute Engine → VM instances → ดูคอลัมน์ Machine type กับ Zone

- ✅ ถ้าตรงเงื่อนไข → หลังหมดเครดิตก็ยังฟรีอยู่ (แค่ต้องผูกบัตรไว้)
- ❌ ถ้าไม่ตรง → จะเริ่มเสียเงิน ~$7-25/เดือน ขึ้นกับขนาดเครื่อง

> n8n ของเรารันแค่ทุก 3 วัน ครั้งละ ~30 วินาที — **e2-micro เอาอยู่สบาย** ถ้าเครื่องปัจจุบันใหญ่เกิน ย้ายลงมาได้

---

## 2️⃣ สำรองข้อมูล n8n

### 🔑 สิ่งที่ห้ามลืมที่สุด — Encryption Key

n8n **เข้ารหัส credentials** (GitHub token, Gemini API key) ด้วยกุญแจที่เก็บใน `~/.n8n/config`

> ถ้าย้ายฐานข้อมูลไปเครื่องใหม่โดยไม่เอากุญแจไปด้วย → **credentials ทั้งหมดใช้ไม่ได้** ต้องกรอกใหม่หมด

---

### วิธีที่ 1 — สำรองทั้งโฟลเดอร์ (ง่ายและครบสุด) ⭐

SSH เข้า VM แล้วรัน:

```bash
# สำรองทั้งโฟลเดอร์ .n8n (มีทั้งฐานข้อมูล + กุญแจเข้ารหัส)
tar -czf ~/n8n-backup-$(date +%F).tar.gz -C ~ .n8n

# ดูว่าไฟล์ถูกสร้างแล้ว
ls -lh ~/n8n-backup-*.tar.gz
```

**ถ้ารันด้วย Docker** ให้หาชื่อ volume ก่อน:
```bash
docker ps                      # ดูชื่อ container
docker cp <ชื่อ_container>:/home/node/.n8n ~/n8n-data
tar -czf ~/n8n-backup-$(date +%F).tar.gz -C ~ n8n-data
```

**ดาวน์โหลดมาเก็บที่เครื่องตัวเอง** (รันบน PowerShell ในเครื่องคุณ):
```powershell
gcloud compute scp <ชื่อ-vm>:~/n8n-backup-*.tar.gz . --zone=<โซน>
```
หรือใช้ปุ่ม **SSH → ⚙️ → Download file** ในหน้า Google Cloud Console ก็ได้

---

### วิธีที่ 2 — Export เป็นไฟล์อ่านได้ (สำรองซ้อน)

```bash
# workflow ทั้งหมด
n8n export:workflow --all --output=~/backup-workflows.json

# credentials แบบถอดรหัสแล้ว (ย้ายเครื่องได้เลย ไม่ต้องใช้กุญแจเดิม)
n8n export:credentials --all --decrypted --output=~/backup-credentials.json
```

> ⚠️ ไฟล์ credentials ที่ถอดรหัสแล้วมี **API key จริง** อยู่ข้างใน — เก็บให้ดี อย่าอัปขึ้น GitHub เด็ดขาด

---

### วิธีที่ 3 — Snapshot ทั้ง VM (กันพลาดที่สุด)

Google Cloud Console → **Compute Engine** → **Snapshots** → **Create Snapshot** → เลือกดิสก์ของ VM

ได้ภาพนิ่งทั้งเครื่อง กู้กลับมาได้ทั้งดุ้น (เสียค่าเก็บเล็กน้อย ~$0.03/GB/เดือน)

---

## 🔄 วิธีกู้คืน (ตอนย้ายเครื่องใหม่)

```bash
# 1. หยุด n8n ก่อน
docker stop n8n        # หรือ  sudo systemctl stop n8n

# 2. แตกไฟล์สำรองกลับ
tar -xzf n8n-backup-2026-07-25.tar.gz -C ~

# 3. เปิด n8n ใหม่
docker start n8n       # หรือ  sudo systemctl start n8n
```

เปิดหน้าเว็บ n8n → workflow และ credentials จะกลับมาครบ

---

## ✅ สิ่งที่คุณมีสำรองอยู่แล้ว

| ข้อมูล | เก็บที่ไหน | ปลอดภัยไหม |
|---|---|---|
| Workflow (โครงสร้าง) | ไฟล์ `n8n-workflow-บทความอัตโนมัติ.json` ในโฟลเดอร์เว็บ + GitHub | ✅ ปลอดภัย |
| บทความที่เขียนไปแล้ว | GitHub (`content/articles/`) | ✅ ปลอดภัย |
| เว็บไซต์ทั้งหมด | GitHub + Vercel | ✅ ปลอดภัย |
| **Credentials** (GitHub token, Gemini key) | **อยู่ในเครื่อง VM เท่านั้น** | ⚠️ **ต้องสำรอง** |

**สรุป:** ต่อให้ VM หายทั้งเครื่อง เว็บไซต์และบทความไม่หาย — เสียแค่ต้องตั้ง n8n ใหม่กับกรอก API key ใหม่ (~15 นาที)

---

## 📅 สิ่งที่ควรทำตอนนี้

1. ☐ เข้า Billing เช็กว่าเหลือกี่วัน — **จดวันหมดไว้**
2. ☐ เช็กว่า VM เป็น e2-micro ในโซนที่ฟรีตลอดชีพหรือไม่
3. ☐ รันคำสั่งสำรองข้อมูล (วิธีที่ 1) แล้วโหลดไฟล์มาเก็บที่เครื่อง
4. ☐ ตั้งเตือนในปฏิทินล่วงหน้า **7 วัน** ก่อนเครดิตหมด
