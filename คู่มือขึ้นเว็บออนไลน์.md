# 🚀 คู่มือเอาเว็บ "หลอมทองพัทยา" ขึ้นออนไลน์ (ฉบับละเอียด ทำตามได้ง่ายๆ)

**เป้าหมาย:** ทำให้คนทั้งโลกเข้าเว็บได้ที่ **หลอมทองพัทยา.com** พร้อมกุญแจล็อก HTTPS (ฟรีทั้งหมด)

**ภาพรวม 3 ช่วง**

1. เอาโค้ดเว็บขึ้น **Vercel** (บริการที่รันเว็บให้ ฟรี เหมาะกับ Next.js ที่สุด)
2. เชื่อม **โดเมน GoDaddy** เข้ากับ Vercel
3. หลังเว็บออนไลน์ → ลงทะเบียนกับ Google

> 💡 "ที่จดโดเมน" (GoDaddy) กับ "ที่รันเว็บ" (Vercel) เป็นคนละอย่าง แยกกันได้ปกติ คุณไม่เสียโดเมน แค่บอกให้โดเมนชี้มาที่ Vercel

**ของที่ต้องเตรียม:** คอมเครื่องที่มีโค้ดเว็บ · อีเมล · บัญชี GoDaddy (มีแล้ว) · เวลาประมาณ 1 ชั่วโมง

---

## 📋 ช่วงที่ 0 — เช็กว่าเว็บรันได้ก่อน (5 นาที)

ก่อน deploy ต้องมั่นใจว่าเว็บ build ผ่าน

**เปิดหน้าต่างคำสั่ง (Terminal) ในโฟลเดอร์เว็บ:**

1. เปิด File Explorer ไปที่โฟลเดอร์ `lomthong-pattaya`
2. คลิกที่ช่องที่อยู่ (address bar) ด้านบน พิมพ์ `cmd` แล้วกด Enter
3. หน้าต่างสีดำจะเปิดขึ้นมา พิมพ์คำสั่งนี้แล้วกด Enter:

```
npm run build
```

- ✅ ถ้าขึ้น **"Compiled successfully"** = เว็บพร้อม deploy ไปช่วงที่ 1 ได้เลย
- ❌ ถ้าขึ้น error สีแดง = ถ่ายรูปหน้าจอส่งมาให้ผมดูก่อน อย่าเพิ่ง deploy

---

## 📤 ช่วงที่ 1 — เอาโค้ดขึ้น Vercel

มี 2 วิธี เลือกวิธีเดียว **แนะนำวิธี A** (ตั้งค่าครั้งเดียว ต่อไปแก้อะไรอัปเดตให้อัตโนมัติ)

### ⭐ วิธี A: ผ่าน GitHub (แนะนำ)

**ขั้นที่ 1 — สมัคร GitHub**
- เข้า https://github.com → กด **Sign up** → สมัครด้วยอีเมล (ฟรี)

**ขั้นที่ 2 — ติดตั้ง GitHub Desktop (โปรแกรมกดปุ่มเอา ไม่ต้องพิมพ์คำสั่ง)**
- เข้า https://desktop.github.com → ดาวน์โหลด → ติดตั้ง → เปิดโปรแกรม → ล็อกอินด้วยบัญชี GitHub เมื่อกี้

**ขั้นที่ 3 — เอาโฟลเดอร์เว็บขึ้น GitHub**
- ในโปรแกรม GitHub Desktop กด **File → Add local repository**
- เลือกโฟลเดอร์ `lomthong-pattaya` → ถ้ามันถามว่าจะ "create a repository" ให้กดตกลง
- กดปุ่ม **Publish repository** (มุมขวาบน)
- ⚠️ เอาเครื่องหมายถูกออกจากช่อง "Keep this code private" ถ้าอยากให้ deploy ง่าย (หรือจะ private ก็ได้ Vercel เชื่อมได้)
- กด Publish → รอสักครู่ โค้ดจะขึ้นไปเก็บบน GitHub

**ขั้นที่ 4 — สมัคร Vercel**
- เข้า https://vercel.com → กด **Sign Up** → เลือก **Continue with GitHub** (ล็อกอินด้วย GitHub เลย ง่ายสุด) → กด Authorize

**ขั้นที่ 5 — Import โปรเจกต์ + Deploy**
- ที่หน้า Vercel กด **Add New… → Project**
- หาโปรเจกต์ `lomthong-pattaya` ในรายการ → กด **Import**
- Vercel จะรู้เองว่าเป็น Next.js (ไม่ต้องตั้งค่าอะไร ปล่อยค่า default ไว้)
- กดปุ่ม **Deploy** → รอ 1-3 นาที
- พอขึ้น 🎉 **Congratulations** = เว็บออนไลน์แล้ว! จะได้ลิงก์ชั่วคราว เช่น `lomthong-pattaya.vercel.app`
- กดลิงก์เปิดดู เช็กว่าเว็บครบทุกหน้า รูปขึ้น ราคาทองวิ่ง

> ✅ ถึงตรงนี้เว็บออนไลน์แล้ว แต่ยังเป็นลิงก์ของ Vercel — ช่วงต่อไปคือเปลี่ยนเป็นชื่อโดเมนของเรา

---

### ⚡ วิธี B: ทางลัดคำสั่งเดียว (เร็ว แต่ไม่อัปเดตอัตโนมัติ)

ถ้าไม่อยากยุ่งกับ GitHub — เปิด cmd ในโฟลเดอร์เว็บ (เหมือนช่วงที่ 0) แล้วพิมพ์:

```
npx vercel
```

- ครั้งแรกจะให้ล็อกอิน → พิมพ์อีเมล → ไปกดยืนยันในอีเมล
- มันจะถามหลายข้อ **กด Enter รัวๆ เพื่อเอาค่า default** ได้เลย (Set up and deploy? Y / project name / directory ./ ฯลฯ)
- รอสักครู่ ได้ลิงก์เว็บออนไลน์มา
- เวลาแก้โค้ดแล้วอยากอัปเดต ต้องพิมพ์ `npx vercel --prod` เองทุกครั้ง

---

## 🌐 ช่วงที่ 2 — เชื่อมโดเมน หลอมทองพัทยา.com

**ขั้นที่ 6 — เพิ่มโดเมนใน Vercel**
- ใน Vercel เปิดโปรเจกต์ → แท็บ **Settings** → เมนู **Domains**
- พิมพ์ `หลอมทองพัทยา.com` → กด **Add**
- Vercel แนะนำให้เพิ่ม `www.หลอมทองพัทยา.com` ด้วย → ตอบตกลง และตั้งให้ **www เป็นตัวหลัก (Primary)** (เพราะโค้ดเราตั้งค่าเป็น www ไว้แล้ว)

**ขั้นที่ 7 — Vercel จะแสดง "DNS records" ที่ต้องเอาไปใส่**
โดยทั่วไปจะเป็นแบบนี้ (ให้ยึดตามที่จอ Vercel บอกจริงเป็นหลัก):

| Type | Name | Value |
|------|------|-------|
| A | @ | `76.76.21.21` |
| CNAME | www | `cname.vercel-dns.com` |

**ขั้นที่ 8 — เอา records ไปใส่ที่ GoDaddy**
- เข้า https://godaddy.com → ล็อกอิน → **My Products**
- หาโดเมน `หลอมทองพัทยา.com` → กด **DNS** (หรือ จัดการ DNS / Manage DNS)
- มาที่ส่วน **Records** แล้วทำ 2 อย่าง:

  1. **หา record Type A ที่ Name = @** ที่มีอยู่เดิม (ของ GoDaddy ที่ชี้ไปหน้า parking) → กด**แก้ไข (ดินสอ)** → เปลี่ยนช่อง Value เป็น `76.76.21.21` → Save
     - ถ้าไม่มี ให้กด **Add** สร้างใหม่: Type = `A`, Name = `@`, Value = `76.76.21.21`, TTL ปล่อย default
  2. กด **Add** เพิ่มอีกอัน: Type = `CNAME`, Name = `www`, Value = `cname.vercel-dns.com`, TTL default → Save
     - ถ้ามี CNAME www เดิมอยู่แล้ว ให้แก้ Value เป็น `cname.vercel-dns.com` แทน

- ⚠️ ถ้ามี record เก่าของ GoDaddy ที่ Name = @ หรือ www ชี้ไปที่อื่น (เช่นหน้า parking / godaddysites) ให้แก้หรือลบทิ้ง ไม่งั้นจะชนกัน

**ขั้นที่ 9 — รอระบบอัปเดต**
- กลับมาที่หน้า Domains ใน Vercel — สถานะจะค่อยๆ เปลี่ยนเป็น ✅ **Valid Configuration**
- ใช้เวลาตั้งแต่ 10 นาที ถึงไม่กี่ชั่วโมง (บางที่ถึง 24 ชม. แต่ปกติเร็ว)
- พอเชื่อมสำเร็จ Vercel จะออกใบรับรอง **HTTPS (กุญแจล็อก) ให้ฟรีอัตโนมัติ**
- เปิด https://หลอมทองพัทยา.com → เว็บสวยๆ ของเราจะขึ้น 🎉

---

## 🔍 ช่วงที่ 3 — หลังเว็บออนไลน์ (ทำให้ติด Google)

1. **Google Search Console** — https://search.google.com/search-console
   - Add property → ใส่ `หลอมทองพัทยา.com` → ยืนยันความเป็นเจ้าของ
   - จะได้ "verification code" มา → **ส่งโค้ดนั้นมาให้ผม** เดี๋ยวผมใส่ในเว็บให้ (ในไฟล์ layout.js มีที่เตรียมไว้แล้ว)
   - เสร็จแล้วส่ง sitemap: ในเมนู Sitemaps พิมพ์ `sitemap.xml` → Submit

2. **Google Business Profile** (สำคัญสุดสำหรับร้านค้า) — https://business.google.com
   - แก้ชื่อร้านให้ตรงกับเว็บ (หลอมทองพัทยา) · ใส่รูป · ที่อยู่ · เบอร์ · เวลาเปิด · ลิงก์เว็บ
   - ขอรีวิวจากลูกค้าจริงเรื่อยๆ

---

## ❓ ปัญหาที่เจอบ่อย + วิธีแก้

- **เปิดโดเมนแล้วยังเป็นหน้า GoDaddy / หน้าว่าง** → DNS ยังไม่อัปเดต รอต่ออีกหน่อย หรือเช็กว่าใส่ record ครบ 2 อันถูกต้อง
- **Vercel ขึ้น "Invalid Configuration"** → ค่าที่ใส่ใน GoDaddy ยังไม่ตรง เช็ก Value ให้ตรงเป๊ะกับที่ Vercel บอก
- **Build failed ตอน deploy** → ลองรัน `npm run build` ในเครื่องก่อน ถ้า error ส่งรูปมาให้ผม
- **แก้เว็บแล้วอยากอัปเดต (วิธี A)** → แค่กด Commit + Push ใน GitHub Desktop → Vercel deploy ใหม่ให้อัตโนมัติ
- **รูปที่อัปโหลดไม่ขึ้นหลัง deploy** → ต้องวางรูปในโฟลเดอร์ `public/images/uploads/...` **ก่อน** push ขึ้น GitHub รูปถึงจะไปด้วย

---

## 📌 สรุปสั้นๆ

```
เช็ก build ผ่าน  →  ขึ้น GitHub  →  Vercel Import+Deploy  →
เพิ่มโดเมนใน Vercel  →  ใส่ DNS ที่ GoDaddy  →  รอเชื่อม  →  เว็บออนไลน์!
```

มีตรงไหนติดขัด ถ่ายรูปหน้าจอส่งมาได้ทุกขั้นตอน เดี๋ยวผมช่วยดูให้ครับ 🙏
