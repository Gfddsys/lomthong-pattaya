# 🤖 ตั้งค่า n8n ให้เขียนบทความขึ้นเว็บอัตโนมัติ

> ระบบนี้ทำงานเองทั้งหมด: **AI เลือกหัวข้อ → เขียนบทความ → ตรวจสอบ → ขึ้นเว็บ**
> คุณไม่ต้องแตะอะไรเลยหลังตั้งค่าเสร็จ

---

## 🧩 ระบบทำงานยังไง

```
n8n (ตั้งเวลา)
   ↓ อ่านว่ามีบทความอะไรแล้วบ้าง (กันเขียนซ้ำ)
   ↓ AI เขียนบทความใหม่ออกมาเป็น JSON
   ↓ ด่านตรวจ — ถ้าของเสีย หยุดทันที เว็บไม่พัง
   ↓ commit ไฟล์ JSON 1 ไฟล์เข้า GitHub
Vercel เห็นว่ามีไฟล์ใหม่ → deploy เอง
เว็บสร้างหน้าบทความ + การ์ด + sitemap ให้เอง
```

**หัวใจคือ:** เว็บถูกปรับให้อ่านบทความจากโฟลเดอร์ `content/articles/` โดยอัตโนมัติแล้ว
n8n จึงแค่ต้องหย่อนไฟล์ JSON เข้าไป **ไฟล์เดียว** ไม่ต้องแตะโค้ดเว็บเลย

---

## 📋 ขั้นตอนตั้งค่า (ทำครั้งเดียว ~15 นาที)

### ขั้น 1 — เอาโค้ดใหม่ขึ้นเว็บก่อน

ต้องทำข้อนี้ก่อน ไม่งั้น n8n ส่งไฟล์ไปแล้วเว็บจะไม่รู้จัก

```powershell
cd C:\Users\Thanathat\.gemini\antigravity\scratch\lomthong-pattaya
npm run build
```

ถ้าผ่าน → Commit + Push ใน GitHub Desktop

---

### ขั้น 2 — สร้าง GitHub Token

Token คือ "กุญแจ" ให้ n8n เขียนไฟล์เข้า repo ได้

1. เข้า https://github.com/settings/tokens
2. กด **Generate new token** → เลือก **Fine-grained token**
3. ตั้งค่า:
   | ช่อง | ใส่อะไร |
   |---|---|
   | Token name | `n8n-lomthong` |
   | Expiration | 1 year |
   | Repository access | **Only select repositories** → เลือก `lomthong-pattaya` |
   | Permissions → Contents | **Read and write** ⚠️ สำคัญ |
4. กด **Generate token** → **ก๊อปเก็บไว้ทันที** (โชว์ครั้งเดียว)

---

### ขั้น 3 — Import workflow เข้า n8n

1. เปิด n8n → มุมขวาบนกด **⋯** → **Import from File**
2. เลือกไฟล์ `n8n-workflow-บทความอัตโนมัติ.json`
3. จะเห็น 9 node เรียงกัน

---

### ขั้น 4 — ใส่ credentials 2 ตัว

**4.1 GitHub**
- คลิก node **"ส่งบทความขึ้น GitHub"**
- ช่อง Credential → **Create New**
- Server: `https://api.github.com`
- User: ชื่อ GitHub ของคุณ
- Access Token: token จากขั้น 2
- **Save**
- ทำแบบเดียวกันที่ node **"ดึงรายชื่อบทความที่มีแล้ว"** (เลือก credential เดิมได้เลย)

**4.2 OpenAI**
- คลิก node **"OpenAI Chat Model"**
- ใส่ API key จาก https://platform.openai.com/api-keys
- **Save**

> อยากใช้ Gemini หรือ Claude แทน? ลบ node "OpenAI Chat Model" ทิ้ง
> แล้วลาก **Google Gemini Chat Model** หรือ **Anthropic Chat Model** มาต่อที่จุดเดิม

---

### ขั้น 5 — แก้ชื่อ repo ของคุณ ⚠️

คลิก node **"ตั้งค่า GitHub"** แล้วแก้ 3 ค่า:

| ช่อง | ใส่อะไร |
|---|---|
| `githubOwner` | ชื่อ GitHub ของคุณ (ดูจาก URL repo) |
| `githubRepo` | `lomthong-pattaya` |
| `githubBranch` | `master` |

---

### ขั้น 6 — ทดสอบ

กด **Execute Workflow** (ทดสอบครั้งเดียว ยังไม่ต้องเปิดอัตโนมัติ)

- ✅ ถ้าเขียวหมด → เข้า GitHub ดูว่ามีไฟล์ใหม่ใน `content/articles/`
- 🛑 ถ้าหยุดที่ **"ตรวจสอบก่อนขึ้นเว็บ"** → อ่านข้อความ error มันบอกชัดว่าอะไรผิด (นี่คือระบบทำงานถูก ไม่ใช่พัง)

รอ Vercel deploy 1-2 นาที → เปิดดูบทความบนเว็บได้เลย

---

### ขั้น 7 — เปิดใช้งานอัตโนมัติ

สลับปุ่ม **Active** มุมขวาบนเป็นสีเขียว → เสร็จ ระบบทำงานเองทุกวันจันทร์ 9 โมง

---

## ⚙️ ปรับแต่งได้

### เปลี่ยนความถี่
คลิก node **"ทุกวันจันทร์ 9 โมง"** แก้ cron:

| ต้องการ | ใส่ |
|---|---|
| สัปดาห์ละ 1 (แนะนำ) | `0 9 * * 1` |
| สัปดาห์ละ 2 | `0 9 * * 1,4` |
| ทุกวัน (เสี่ยง) | `0 9 * * *` |

### 🔒 เปลี่ยนเป็นโหมด "ร่าง" (ปลอดภัยที่สุด)

ถ้าอยากให้ AI เขียนไว้ก่อน แต่**ยังไม่ขึ้นเว็บ**จนกว่าคุณจะอนุมัติ:

คลิก node **"ตรวจสอบก่อนขึ้นเว็บ"** หาบรรทัดนี้ (ประมาณบรรทัด 60):

```js
a.published = true;
```

แก้เป็น:

```js
a.published = false;
```

ผลลัพธ์: บทความถูกเขียนและ commit เข้า GitHub ตามปกติ แต่**ไม่แสดงบนเว็บ**
เมื่อคุณอ่านแล้วพอใจ ก็เข้าไปแก้ `"published": false` เป็น `true` ในไฟล์ JSON นั้น → บทความขึ้นเว็บ

---

## 🛡️ ด่านตรวจป้องกันอะไรบ้าง

ทดสอบมาแล้วทั้งหมด — ถ้า AI ส่งของเสีย ระบบจะ**หยุดก่อนถึง GitHub** เว็บไม่พัง:

| กรณี | ผลลัพธ์ |
|---|---|
| AI ตอบไม่ใช่ JSON | 🛑 หยุด |
| AI ห่อด้วย ` ```json ` | ✅ ตัดออกให้เอง |
| AI พูดนำหน้า JSON | ✅ ตัดออกให้เอง |
| slug ซ้ำบทความเดิม | 🛑 หยุด |
| slug เป็นภาษาไทย/มีเว้นวรรค | 🛑 หยุด |
| แทรก `<script>` หรือ type แปลกปลอม | 🛑 หยุด |
| บทความสั้นเกินไป (< 8 บล็อก) | 🛑 หยุด |
| ขาดฟิลด์สำคัญ | 🛑 หยุด |
| ใส่ path รูปมั่ว | ✅ แก้เป็นรูปสำรองให้เอง |

และต่อให้มีไฟล์เสียหลุดเข้าไปในโฟลเดอร์จริง **เว็บก็ยังไม่พัง** เพราะ `lib/articles.js`
จะข้ามไฟล์นั้นแล้วขึ้น warning แทน (ทดสอบแล้วเช่นกัน)

---

## ⚠️ สิ่งที่ต้องรู้ก่อนเปิดใช้

**1. Google ไม่ได้ห้าม AI เขียน — แต่ห้ามเนื้อหาไร้ค่าที่ผลิตทีละมากๆ**
สัปดาห์ละ 1-2 บทความ = ปลอดภัย · วันละหลายบทความ = เสี่ยงโดนลงโทษ

**2. AI แต่งประสบการณ์จริงไม่ได้**
prompt สั่งห้ามแต่งไว้แล้ว แต่นั่นแปลว่าบทความจะเป็นข้อมูลกลางๆ
**ทุก 2-3 บทความ ลองเข้าไปเติมเคสจริงจากหน้าร้านสัก 2-3 ประโยค** — จุดนี้แหละที่ทำให้ชนะคู่แข่งที่ใช้ AI ล้วน

**3. บทความไม่ใช่ตัวชี้ขาดอันดับของร้าน Local**
เรียงตามน้ำหนักจริง: **Google Business Profile + รีวิว** > NAP ตรงกันทุกที่ > บทความ
ระบบนี้คือขาที่ 3 — อย่าลืมทำขา 1 กับ 2 ด้วย

**4. ค่าใช้จ่าย**
OpenAI ประมาณ 3-8 บาท/บทความ (gpt-4o) → สัปดาห์ละ 1 บท ≈ 30 บาท/เดือน

---

## 🆘 แก้ปัญหา

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| GitHub ขึ้น 404 | `githubOwner` หรือ `githubRepo` ผิด — เช็ก URL repo |
| GitHub ขึ้น 403 / 401 | Token ไม่มีสิทธิ์ **Contents: Read and write** |
| GitHub ขึ้น 422 | มีไฟล์ชื่อนี้อยู่แล้ว (AI เขียนซ้ำ) — รันใหม่ได้เลย |
| หยุดที่ด่านตรวจบ่อย | AI ตอบไม่เป็น JSON — ลดค่า temperature เป็น 0.4 |
| บทความไม่ขึ้นเว็บ | เช็ก Vercel ว่า deploy ผ่านไหม + ไฟล์ต้องมี `"published": true` |
